# 버전 관리 전략

## 개요

`create-hua-ux`는 생성되는 프로젝트에 사용할 의존성 버전을 관리합니다. 버전 관리 방식은 패키지에 따라 다릅니다.

## 버전 관리 방식

### 1. @hua-labs/hua-ux 버전 (자동화됨) ✅

**방식**: 빌드 시점 자동 동기화

**동작 흐름**:
```
1. 빌드 시점: scripts/generate-version.ts 실행
   ↓
2. hua-ux/package.json에서 버전 읽기 (예: 0.1.0)
   ↓
3. src/version.ts 생성 (export const HUA_UX_VERSION = '^0.1.0')
   ↓
4. 런타임: getHuaUxVersion() 함수
   - 모노레포 감지 → 'workspace:*'
   - npm 배포 후 → HUA_UX_VERSION 상수 사용
```

**업데이트 방법**:
```bash
# 1. hua-ux 버전 업데이트
cd packages/hua-ux
npm version minor  # 0.1.0 → 0.2.0
pnpm run build
npm publish --access public

# 2. create-hua-ux 재빌드 (자동으로 새 버전 반영)
cd packages/create-hua-ux
pnpm run build  # scripts/generate-version.ts가 자동으로 0.2.0 읽음

# 3. create-hua-ux 재배포
npm version patch
npm publish --access public
```

**결과**: 새로 생성되는 프로젝트는 자동으로 최신 버전(`^0.2.0`) 사용

### 2. Next.js/React 버전 (상수 파일로 관리) ✅

**현재 상태**:
- Next.js: `16.0.10`
- React: `19.2.1`
- React DOM: `19.2.1`

**위치**: `packages/create-hua-ux/src/constants/versions.ts`

**업데이트 방법**:
1. `src/constants/versions.ts` 파일에서 버전 수정
2. 재빌드 및 재배포

**장점**:
- ✅ 한 곳에서 관리 (중앙화)
- ✅ 타입 안전성
- ✅ 환경 변수보다 명확
- ✅ IDE 자동완성 지원

### 3. 기타 의존성 버전

**현재 상태**:
- zustand: `^5.0.8`
- tailwindcss: `^4.1.13`
- typescript: `^5.9.3`

**업데이트 방법**: `src/utils.ts`에서 직접 수정

## 버전 업데이트 시나리오

### 시나리오 1: hua-ux만 업데이트

**질문**: `@hua-labs/hua-ux`를 업데이트하면 자동으로 반영되나요?

**답변**: ✅ **네, 자동으로 반영됩니다!**

**절차**:
1. `hua-ux` 패키지 버전 업데이트 및 배포
2. `create-hua-ux` 재빌드 (`pnpm run build`)
   - `scripts/generate-version.ts`가 자동으로 새 버전 읽음
   - `src/version.ts` 파일이 새 버전으로 업데이트됨
3. `create-hua-ux` 재배포
4. **결과**: 새로 생성되는 프로젝트는 자동으로 최신 `hua-ux` 버전 사용

**예시**:
```bash
# hua-ux: 0.1.0 → 0.2.0
cd packages/hua-ux
npm version minor
npm publish

# create-hua-ux 재빌드
cd packages/create-hua-ux
pnpm run build  # 자동으로 ^0.2.0 반영
npm publish

# 사용자가 새 프로젝트 생성
npm create hua-ux my-app
# → package.json에 "@hua-labs/hua-ux": "^0.2.0" 자동 포함
```

### 시나리오 2: Next.js/React 업데이트

**질문**: Next.js나 React를 업데이트하려면?

**답변**: ⚠️ **수동 업데이트 필요**

**절차**:
1. `src/utils.ts`의 `generatePackageJson()` 함수에서 버전 수정
2. 재빌드 및 재배포

**예시**:
```typescript
// src/constants/versions.ts
export const NEXTJS_VERSION = '16.0.10';  // → '16.1.0'으로 수정
export const REACT_VERSION = '19.2.1';    // → '19.3.0'으로 수정
export const REACT_DOM_VERSION = '19.2.1'; // → '19.3.0'으로 수정

// src/utils.ts는 자동으로 반영됨
```

## 모노레포 vs npm 배포

### 모노레포 내부 (개발 환경)

**동작**:
- `getHuaUxVersion()`이 `pnpm-workspace.yaml` 감지
- `workspace:*` 반환
- 생성된 프로젝트는 모노레포의 `hua-ux` 패키지 사용

**장점**:
- 개발 중 즉시 변경사항 반영
- 별도 배포 없이 테스트 가능

### npm 배포 후 (프로덕션)

**동작**:
- `getHuaUxVersion()`이 빌드 시 생성된 `HUA_UX_VERSION` 상수 사용
- 생성된 프로젝트는 npm의 `@hua-labs/hua-ux` 패키지 사용

**장점**:
- 안정적인 버전 관리
- 사용자가 명시적으로 업데이트 가능

## 권장 사항

### 1. 버전 동기화

- `create-hua-ux`와 `@hua-labs/hua-ux`의 버전을 동기화하는 것이 좋음
- 또는 `create-hua-ux`에서 `@hua-labs/hua-ux`의 최신 버전을 자동으로 가져오는 로직 사용 (현재 구현됨)

### 2. Next.js/React 버전 관리

**✅ 구현 완료: 옵션 B (상수 파일 분리)**

**구현 위치**: `src/constants/versions.ts`

**장점**:
- ✅ 한 곳에서 관리 (중앙화)
- ✅ 타입 안전성
- ✅ IDE 자동완성 지원
- ✅ 환경 변수보다 명확

**업데이트 시**:
1. `src/constants/versions.ts` 수정
2. 재빌드 및 재배포

### 3. 배포 전 확인

- [ ] `@hua-labs/hua-ux`가 npm에 배포되어 있는지 확인
- [ ] 생성된 프로젝트의 `package.json`에 올바른 버전 포함 확인
- [ ] `workspace:*`가 아닌 실제 버전(`^0.1.0`)인지 확인

## 결론

**✅ 자동화된 부분**:
- `@hua-labs/hua-ux` 버전은 자동으로 동기화됨
- `hua-ux` 업데이트 → `create-hua-ux` 재빌드 → 자동 반영

**⚠️ 수동 관리 필요**:
- `src/constants/versions.ts`에서 Next.js/React 버전 업데이트
- 기타 의존성 버전 (zustand, tailwindcss 등)

**💡 권장 워크플로우**:
1. `hua-ux` 업데이트 및 배포
2. `create-hua-ux` 재빌드 (자동 버전 반영)
3. 필요시 Next.js/React 버전 수동 업데이트
4. `create-hua-ux` 재배포
