import React from 'react';

export default function MessagePlaceholder() {
  return (
    <div 
      className="w-full max-w-[768px] animate-pulse"
      style={{ 
        minHeight: '300px',
      }}
    />
  );
} 